import React, { useContext } from "react";
import { AuthContext } from "../../context/authContext";

const WatchlistBtn = () => {
  //const { logout } = useContext(AuthContext);

  return (
    <button className="watchlistBtn" title="Watchlist"/>
  );
};

export default WatchlistBtn;
